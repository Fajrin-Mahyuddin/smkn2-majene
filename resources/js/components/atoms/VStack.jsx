import { Flex } from "antd";
import React from "react";
import styled from "styled-components";

const Wrapper = styled(Flex)``;

const VStack = () => {
	return <Flex>VStack</Flex>;
};

export default VStack;
